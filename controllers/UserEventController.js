const { Category } = require('../models')

const GetUserEvents = async (req, res) => {
  try {
  } catch (error) {
    throw error
  }
}

const GetAttendees = async (req, res) => {
  try {
  } catch (error) {
    throw error
  }
}

module.exports = {
  GetUserEvents,
  GetAttendees
}
